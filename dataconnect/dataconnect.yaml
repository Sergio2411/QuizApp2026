specVersion: "v1"
serviceId: "quizapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "quizapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
